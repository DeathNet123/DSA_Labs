#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int fine_division(int index, int sum, int size)
{
    //base case..
    if(index == size)
    {
        
    }
}


int main(void) 
{
    int size, temp;
    vectore <int> array;
    cin>>size;
    for(int idx = 0; idx < size; idx++)
    {
        cin>>temp;
        array.push_back(temp);
    }
    
    
    return 0;
}
